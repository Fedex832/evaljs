'use strict';

/*
 * Grid
 * @param {number} xAxis
 * @param {mumber} yAxis
 */


var Grid = function Grid(xAxis, yAxis) {
  this.xAxis = xAxis;
  this.yAxis = yAxis;
}

/*
 * run
 * @return this
 */

Grid.prototype.run = function() {
  return this.Grid;
}
